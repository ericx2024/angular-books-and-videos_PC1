import { BooksVideosEntity } from './books-videos.entity';

describe('BooksVideosEntity', () => {
  it('should create an instance', () => {
    expect(new BooksVideosEntity()).toBeTruthy();
  });
});
